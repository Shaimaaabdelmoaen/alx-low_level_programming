#include<stdio.h>
/**
 * main - Entry - point
 * Return: 0 (success)
 */
int main(void)
{
	int c;
	char d;

	c = 0;
	d = 'a';
	while
		(c < 10) {
			putchar(c);
			c++;
		}
	while
		(d < 'g') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
